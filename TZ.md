# ТЗ — Доработка «Калькулятор процентовки»

Документ фиксирует согласованный план Фазы 1 доработки приложения.  
Дата: 2026-06-22 · Статус: **Программы переработаны (§11.1 ✅); дальше — Журнал/Профиль/Отчёты/Админка (§11.2–11.8)**

---

## 1. Общий контекст

Приложение — PWA для пауэрлифтинга. Текущий стек: чистый HTML/CSS/JS (ES-модули),
хостинг GitHub Pages, данные в `localStorage`.

**Фаза 1** — весь функционал локально, без бэкенда.  
**Фаза 2** — бэкенд Supabase, профили тренер/атлет, синхронизация между устройствами
(реализуется после того как Фаза 1 обкатана; токены/ключи Supabase будут предоставлены позже).

---

## 2. Навигация — новая структура (5 разделов)

```
┌────────────┬───────────┬──────────┬──────────┬──────────┐
│Калькулятор │ Программы │  Журнал  │Подсобки  │ Профиль  │
└────────────┴───────────┴──────────┴──────────┴──────────┘
```

Раздел **«Цикл 4 недели»** из текущей навигации **убирается** — его содержимое
переезжает в переработанный Калькулятор.

---

## 3. Экран 1 — Калькулятор (переработка)

### 3.1 Что убрать / изменить

- **Убрать строку 105%** из всех таблиц программ и цикла.
- **Оставить** плитку «Цель цикла · 105%» как числовое значение (не строка таблицы).
- Раздел «Цикл» (с переключением недели/дня) заменяется 4 вертикальными таблицами
  без переключения — все 4 недели видны сразу на одном экране.

### 3.2 Макет (сверху вниз)

```
┌──────────────────────────────────────────────────┐
│  [Присед]      [Жим лёжа]     [Становая]         │  ← поля ввода 1ПМ
│  Сумма 1ПМ: 580 кг     Цель цикла · 105%: 609 кг │  ← плитки
├──────────────────────────────────────────────────┤
│  НЕДЕЛЯ 1 · средняя                               │
│  %      Присед    Жим      Тяга                  │
│  65%    130       91       156                   │
│  75%    150       105      180                   │
│  85%    170       119      204                   │
├──────────────────────────────────────────────────┤
│  НЕДЕЛЯ 2 · средняя                               │
│  70% / 80% / 90%  ...                            │
├──────────────────────────────────────────────────┤
│  НЕДЕЛЯ 3 · тяжёлая                               │
│  75% / 85% / 95%  ...                            │
├──────────────────────────────────────────────────┤
│  НЕДЕЛЯ 4 · лёгкая                                │
│  40% / 50% / 60%  ...                            │
└──────────────────────────────────────────────────┘
```

### 3.3 Матрица недель (итоговая, без 105%)

| Неделя | Характер  | Подходы (%)      |
|--------|-----------|-----------------|
| Н1     | средняя   | 65 · 75 · 85    |
| Н2     | средняя   | 70 · 80 · 90    |
| Н3     | тяжёлая   | 75 · 85 · 95    |
| Н4     | лёгкая    | 40 · 50 · 60    |

### 3.4 Типы тренировок (влияют на количество столбцов)

Тип выбирается при создании **программы** (не в калькуляторе глобально):

| Тип          | Упражнения                        |
|--------------|-----------------------------------|
| Троеборье    | Присед + Жим лёжа + Становая тяга |
| Двоеборье    | Жим лёжа + Становая тяга          |
| Одно движение | Только одно (выбор: присед / жим / тяга) |

Калькулятор на первом экране по умолчанию показывает троеборье.
Если у пользователя есть активная программа — берёт тип из неё.

### 3.5 История

Остаётся как есть (снимки 1ПМ с датой, загрузить / удалить).

---

## 4. Экран 2 — Программы

### 4.1 Структура

```
Мои программы              Шаблоны (только просмотр)
─────────────────          ─────────────────────────
[+ Создать]                [Наш 4-нед. цикл  →]
                           [5/3/1             →]
[Моя прога 1  →]           [Шейко             →]
[Моя прога 2  →]           [Куб               →]
```

### 4.2 Шаблоны (зашиты нами)

- **Наш 4-нед. цикл** — текущая матрица (Н1–Н4, см. §3.3)
- **5/3/1** — классическая программа Джима Вендлера (3 рабочих подхода + АМРАП)
- **Шейко** — базовый блок Шейко (большой объём, % от 1ПМ)
- **Куб** — Cube Method Брэндона Лилли (ротация: взрывная/тяжёлая/объёмная)

Шаблоны: **только просмотр**. Кнопка «Взять за основу» создаёт редактируемую копию
в разделе «Мои программы».

### 4.3 Конструктор программы

При создании/редактировании задаётся:
- Название
- Тип: Троеборье / Двоеборье / Одно движение (+ выбор упражнения)
- Количество недель (от 1 до 12)
- Для каждой недели: название/характер + список подходов (% и повт для каждого упражнения)

**Правило:** 105% (проходка) в конструкторе недоступна — максимум 100%.
Проходка = соревнования, не тренировка.

---

## 5. Экран 3 — Журнал

Два подраздела (переключение табом):

### 5.1 ПЛАН

- Выбор активной программы
- Навигация по неделям и дням
- Отображение: упражнение → список подходов (% · кг · повт)
- Только просмотр (редактирование — в Конструкторе)

### 5.2 ФАКТ (дневник выполнения)

- Кнопка «Начать тренировку» → открывает текущий день плана
- Для каждого подхода:
  - Факт. повторения (число)
  - Факт. вес, кг (с подсказкой «на снарядах» — кратно 2,5 кг)
  - Качество: 1–5 (Ужасно / С ошибками / Нормально / Хорошо / Отлично)
  - Ошибка: выпадающий список из каталога `ERRORS[liftKey]` (необязательно)
  - Заметка: свободный текст (необязательно)
- Кнопка «Завершить тренировку» → запись в историю

### 5.3 История тренировок

- Список прошлых тренировок (дата, программа, неделя/день, сумма кг)
- Просмотр деталей каждой

---

## 6. Экран 4 — Подсобки

Два подраздела:

### 6.1 КАТАЛОГ

Упражнения разбиты по категориям:

| Категория      | Примеры                                     |
|---------------|---------------------------------------------|
| Жим / плечи   | Жим узким хватом, Дипы, Разводка, ОХП      |
| Присед / ноги | Фронтальный присед, Жим ногами, Выпады     |
| Тяга / спина  | Тяга в наклоне, Тяга с плинтов, Румынская  |
| Корпус        | Гиперэкстензии, Планка, «Доброе утро»       |
| ОФП           | Подтягивания, Отжимания, Фермерская ходьба |

- Карточка упражнения: название, мышцы, как выполнять, с чем помогает
- Кнопка «+ Добавить своё» — пользователь создаёт кастомное упражнение

### 6.2 ПРОБЛЕМЫ → ПОДСОБКИ

Берём каталог ошибок из `content.js` (ERRORS) и расширяем:

```
Присед:
  ├── Не встал / завалился    → [список подсобок]
  ├── Округлил спину          → [список подсобок]
  ├── Колени ушли внутрь      → [список подсобок]
  ├── Пятки оторвались        → [список подсобок]
  └── Не досел                → [список подсобок]

Жим лёжа:
  ├── Не коснулся груди       → [список подсобок]
  ├── Оторвал таз             → [список подсобок]
  ├── Перекос штанги          → [список подсобок]
  └── Болтанка в локтях       → [список подсобок]

Становая тяга:
  ├── Согнул руки             → [список подсобок]
  ├── Округлил поясницу       → [список подсобок]
  ├── Штанга ушла вперёд      → [список подсобок]
  ├── Не довёл таз            → [список подсобок]
  └── Рывок спиной            → [список подсобок]
```

**Связи «проблема → подсобки» зашиваем мы** (не настраивается пользователем).

---

## 7. Экран 5 — Профиль (Фаза 1, локально)

- Имя, фото, возраст
- Вес тела с историей замеров
- Тайминг тренировки (начало/конец/длительность)
- Роль: **Тренер / Атлет** — сохраняется локально (понадобится в Фазе 2)

> Полноценное разделение тренер/атлет (тренер видит всех своих атлетов
> и их журналы) — реализуется в **Фазе 2** вместе с бэкендом Supabase.

---

## 8. Открытые вопросы (нужны ответы перед реализацией)

| # | Вопрос | Статус |
|---|--------|--------|
| 1 | Тип тренировки — выбирается при создании программы? | ✅ Да |
| 2 | Калькулятор по умолчанию — троеборье? | ⏳ ожидает подтверждения |
| 3 | Пустые таблицы (нет 1ПМ) — показывать «—» или скрывать? | ⏳ ожидает ответа |
| 4 | Список подсобок для зашивки — предоставит пользователь | ⏳ ожидает списка |
| 5 | Supabase токены для Фазы 2 | ⏳ будут предоставлены позже |

---

## 9. Порядок реализации Фазы 1

| # | Задача | Файлы |
|---|--------|-------|
| 1 | Калькулятор: 4 таблицы недель, убрать 105%, новая матрица | `config.js`, `calc.js`, `styles.css` |
| 2 | Новая навигация (5 разделов, убрать «Цикл») | `nav.js`, `index.html` |
| 3 | Программы: шаблоны + конструктор | `programs.js`, `programview.js`, `templates.js` |
| 4 | Журнал: план + факт + история | `diary.js`, `diaryview.js` |
| 5 | Подсобки: каталог + проблемы + добавить своё | `accessories.js`, `accessview.js` |
| 6 | Профиль: роль тренер/атлет | `profile.js` (дополнить) |
| 7 | Обновить Service Worker | `sw.js` → `pct-v10` |

---

## 10. Технические правила (не меняются)

- Стек: чистый HTML/CSS/JS (ES-модули), без фреймворков и сборщиков
- Хранение Фаза 1: `localStorage` (ключи по схеме `pct.*` / `profile.*`)
- Деплой: `git push` в `main` → GitHub Pages
- После любых изменений файлов: поднимать `CACHE` в `sw.js`
- Тесты: `npm test` (Node ESM, папка `tests/`)
- Локальный сервер: `python -m http.server 8000`

---

## 11. Фаза 1.5 — переработка «Программ» и далее (план от 2026-06-22)

Базовая Фаза 1 завершена. Ниже — согласованная переработка.
**Приоритет сейчас — раздел «Программы» (11.1).** Остальное (11.2–11.5) — после, в этом порядке.
Тренер↔Атлет через БД — отложено (11.6).

### 11.1 Программы — новая иерархия (✅ СДЕЛАНО)

> Готово: мастер Уровень→Дисциплина→Упор; неделя→дни→подходы + подсобки к движениям;
> конструктор; миграция старых программ; журнал переведён на навигацию неделя+день.
> Контент — черновик (правится в §11.8). Тесты `npm test` (79), `CACHE=pct-v12`.

Выбор программы — «мастер» из трёх шагов, затем готовая редактируемая программа:

```
Шаг 1. Уровень подготовки   →  Начинающий · Любитель · Профессионал
Шаг 2. Дисциплина           →  Троеборье · Двоеборье · Жим · Становая
Шаг 3. Упор (акцент цикла)  →  напр. для становой: срыв с пола / дожим (локаут) / скорость
        ↓
Готовый шаблон с весами от 1ПМ  →  «взять за основу» → редактируемая «моя программа»
```

- **Двоеборье** = жим + становая.
- **Упор** — что усиливаем в этом цикле (набор зависит от дисциплины).
- Готовую программу можно править: менять подходы И **заводить подсобки к основным упражнениям**.

**Дни внутри недели.** Программа теперь: неделя → **дни** (по умолч. 3 дня/нед) → упражнения → подходы.
Каждый день — свои основные движения (по дисциплине и сплиту) + подсобки.

**Новая модель данных программы/шаблона:**
```
{
  id, name,
  level: "novice"|"amateur"|"pro",               // тип подготовки
  discipline: "pl"|"bp_dl"|"bench"|"deadlift",    // троеборье/двоеборье/жим/становая
  focus: "<ключ упора>",                          // акцент цикла
  weeks: [
    { kind, days: [
        { name, exercises: [
          { key:"squat"|"bench"|"deadlift", sets:[{pct,reps}],  // основное движение (вес от 1ПМ)
            acc:[{name, sets, reps}] }                          // подсобки (без 1ПМ: подходы×повт)
        ] }
    ] }
  ]
}
```
Заменяет прежние `type`/`lift` → `discipline`/`focus`. Старые сохранённые программы
мигрируются (regression-тест): старые `weeks[{sets}]` → один день с движениями по типу.

**Калькулятор** не трогаем — 4 таблицы недель от 1ПМ остаются «быстрым» экраном.

### 11.2 Журнал — доработки (СЛЕДУЮЩЕЕ — после 11.1)
- ФАКТ показывает **какая неделя/день сейчас**: прогрессия по числу завершённых тренировок
  (сделал 2 трен. → «Начать» открывает 3-ю тренировку недели 1 и т. д.).
- **Вес тела вводится в самой тренировке** (не в профиле).
- **Тайминг в тренировке**: начало = время устройства (правится), конец = вручную
  или автоматически по «Завершить тренировку».
- ПЛАН/ФАКТ — навигация по неделям И дням.

### 11.3 Профиль — упрощение (после 11.1)
- Оставить: ФИО, дата рождения, фото (аватар).
- **Убрать из профиля**: тайминг/«тренировка сегодня» (переезжает в тренировку); роль Тренер/Атлет — пока не нужна.
- **Вес тела**: предзаполняется из последней тренировки, правится вручную; история остаётся.

### 11.4 Отчёты — новый раздел (после 11.1)
- Среднее время тренировки и диапазон.
- + придумать метрики: объём по неделям, прогресс 1ПМ, частота тренировок, сильные/слабые движения…

### 11.5 Подсобки
- Каталог используется при «заведении подсобок к упражнению» в программе (11.1).

### 11.6 Тренер ↔ Атлет через БД — ОТЛОЖЕНО
- Сначала всё без тренеров/атлетов. Затем проектируем БД-модель.
- ⚠️ Когда дойдём: **задать пользователю ключевые «глобальные» вопросы** (кто кого видит,
  привязка, синхронизация). Сейчас — не трогаем.

### 11.7 Открытые решения (фиксируются перед сборкой 11.1)

| # | Вопрос | Решение |
|---|--------|---------|
| A | Наполнение веток | ✅ Черновик от меня; контент = **редактируемые данные** (не логика), позже правится в админке (11.8) |
| B | Сплит троеборья по умолчанию (3 дня) | ✅ Движение на день: Д1 присед · Д2 жим · Д3 становая |
| C | Подсобки крепятся к основному движению дня | ✅ да (по словам пользователя) |
| D | Уровень/дисциплина/упор — структура data-driven, контент заменяемый | ✅ да |

### 11.8 Админка контента — НОВЫЙ ПУНКТ (после 11.1–11.5)

Пользователь = админ сайта. Вся **логика работает автоматически**, а **контент — редактируемые данные**:
- шаблонные программы (схемы недель/дней/подходов под уровень × дисциплину × упор);
- тексты и описания (для чего упражнение/упор нужен, что на что влияет);
- каталог подсобок и связи «проблема → подсобки»;
- тексты типовых ошибок.

Поэтому **уже сейчас** весь контент держим как чистые данные (модули `templates`/`content`/
`accessories`), отделённые от движка. Позже — **админ-панель** для правок + слой
переопределений в `localStorage` (правки админа поверх встроенных дефолтов). Строим
ПОСЛЕ переработки 11.1 (и остальных 11.2–11.5).
